<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    @if (User.Identity.IsAuthenticated)
    {
        <h1>Ласкаво просимо до сайту, @User.Identity.Name!</h1>
    }
    <style>
        .button {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
            background-color: #4caf50; /* Колір фону кнопки */
            color: #fff; /* Колір тексту кнопки */
            border: 2px solid #4caf50; /* Обрамлення кнопки */
            border-radius: 5px;
            transition: background-color 0.3s;
        }

            .button:hover {
                background-color: #45a049; /* Змінений колір фону при наведенні курсору */
                color: #fff;
            }
    </style>
</head>
<body>
    <a href="https://localhost:44351" class="button">Головна сторінка</a>
</body>
</html>
